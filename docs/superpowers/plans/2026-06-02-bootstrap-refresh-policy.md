# Bootstrap Refresh Policy Plan

Status: Implemented

## Steps

- [x] Preserve existing `~/.skills/using-skill-rag` by default.
- [x] Add lifecycle `refresh_bootstrap` support.
- [x] Add CLI `install --refresh-bootstrap`.
- [x] Report `bootstrap_refreshed` separately from `bootstrap_installed`.
- [x] Document the option in both READMEs.
- [x] Remove the resolved bootstrap refresh item from the tech debt tracker.
- [x] Add lifecycle and CLI tests.
- [x] Run focused and full test suites.
